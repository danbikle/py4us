%h2 Class04 Answer:
%h2 Ensure that you are running Anaconda Python:

%p When I issue the python command I should see this banner:

.syntax
  %pre
    %code.bash
      $ python
      Python 3.5.2 |Anaconda 4.2.0 (64-bit)| (default, Jul  2 2016, 17:53:06) 
      [GCC 4.4.7 20120313 (Red Hat 4.4.7-1)] on linux
      Type "help", "copyright", "credits" or "license" for more information.
      >>> 

%p If I do not see that banner, or if I worry that my copy of Anaconda is corrupt, I should install or reinstall Anaconda with some shell commands:

.syntax
  %pre
    =render 'class04ana2'

%p Notice that I installed Anaconda 4.2.0 from Anaconda3-4.2.0-Linux-x86_64.sh

%p That version works well with Flask-Restful.

%p I tried to install Flask-Restful with Anaconda 4.4.0 and encountered errors.

%p For now, I should avoid installing Anaconda 4.4.0.

%p
  %a(href='class04#p1')
    Class04 Lab

